---js
const eleventyNavigation = {
	key: "Map",
	order: 3
};
---
<h1>Countries We've Visited</h1>

<script src="/js/svg-pan-zoom/svg-pan-zoom.min.js"></script>

{# From https://github.com/flekschas/simple-world-map #}
{% map "simple_world_map" %}

<hr>

<ul class="map-details">
{%- for country in collections.posts | visitedCountries %}
    <li id="{{country.detailAnchor}}">
        <span class="map-detail-name">{{country.name}}:</span>
        <span class="map-detail-details">{{country.visited}}</span>
        {%- if country.linkable %}
            [<a href="/tags/{{country.tag}}">Posts</a>]
        {%- endif%}
        {%- if country.map %}
            [<a href="/maps/{{country.map}}/">Map</a>]
        {%- endif%}
    </li>
{%- endfor %}
</ul>
